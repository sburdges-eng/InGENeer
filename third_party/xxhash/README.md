# xxHash (v0.8.3)

Mirrored verbatim from https://github.com/Cyan4973/xxHash @ v0.8.3.
Do not edit `include/xxhash.h` in-tree; re-vendor from upstream for updates.

Consumed by `libs/pointcloud_core` (XXH3-64 per-node payload checksums) via `octree_checksum.cpp` only.
